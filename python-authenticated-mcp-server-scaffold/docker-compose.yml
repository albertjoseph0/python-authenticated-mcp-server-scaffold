services:
  mcp-server:
    build: .
    container_name: mcp-server
    restart: unless-stopped
    ports:
      - "8788:8788"
    env_file:
      - .env
    environment:
      - PORT=8788
      - RESOURCE_SERVER_URL=https://mcp.albertjoseph.dev/
